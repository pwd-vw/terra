export function Dashboard() {
  return (
    <div className="page-enter" style={{ padding: '40px', minHeight: 'calc(100vh - 56px)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <div style={{ textAlign: 'center' }}>
        <div style={{ fontFamily: 'var(--display)', fontSize: 60, color: 'var(--glow)', marginBottom: 16 }}>DASHBOARD</div>
        <p style={{ color: 'var(--text3)', fontFamily: 'var(--mono)', fontSize: 11, letterSpacing: '0.2em' }}>COMING IN PHASE 1 — TERRA PIPELINE LIVE</p>
      </div>
    </div>
  )
}
